load("@rules_cc//cc:defs.bzl", "cc_library")

licenses(["notice"])

cc_library(
    name = "glad",
    srcs = ["glad.cc"],
    hdrs = [
        "glad.h",
        "linmath.h",
    ],
    include_prefix = "glad",
    visibility = ["//visibility:public"],
    deps = [
        "//third_party/khrplatform:khr",  # keep
    ],
)
