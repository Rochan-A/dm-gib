load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "report",
    hdrs = [
        "report.h",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "//third_party/glad",
        "//util/time",
        "@fmt",
        "@tracy",  # keep
    ],
)
